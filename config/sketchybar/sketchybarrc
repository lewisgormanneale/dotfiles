# Sketchybar Configuration File

# https://github.com/FelixKratz/dotfiles

# directory: https://github.com/mhuggins7278/dotfiles/tree/bb2c91a77edb248a890b7e5c6cf37893647beb0d

PLUGIN_DIR="$CONFIG_DIR/plugins"

##### Catppuccin Mocha Color Palette

# https://github.com/catppuccin/catppuccin

ROSEWATER=0xfff5e0dc
FLAMINGO=0xfff2cdcd
PINK=0xfff5c2e7
MAUVE=0xffcba6f7
RED=0xfff38ba8
MAROON=0xffeba0ac
PEACH=0xfffab387
YELLOW=0xfff9e2af
GREEN=0xffa6e3a1
TEAL=0xff94e2d5
SKY=0xff89dceb
SAPPHIRE=0xff74c7ec
BLUE=0xff89b4fa
LAVENDER=0xffb4befe
TEXT=0xffcdd6f4
SUBTEXT1=0xffbac2de
SUBTEXT0=0xffa6adc8
OVERLAY2=0xff9399b2
OVERLAY1=0xff7f849c
OVERLAY0=0xff6c7086
SURFACE2=0xff585b70
SURFACE1=0xff45475a
SURFACE0=0xff313244
BASE=0xff1e1e2e
MANTLE=0xff181825
CRUST=0xff11111b

##### Bar Appearance

sketchybar --bar position=top height=20 blur_radius=30 color=$CRUST

##### Changing Defaults

default=(
padding_left=5
padding_right=5
icon.font="Hack Nerd Font:Bold:17.0"
label.font="Hack Nerd Font:Bold:14.0"
icon.color=$TEXT
  label.color=$TEXT
icon.padding_left=4
icon.padding_right=4
label.padding_left=4
label.padding_right=4
)
sketchybar --default "${default[@]}"

##### Adding AeroSpace Workspace Indicator

# Display current AeroSpace workspace


sketchybar --add item aerospace left \
           --set aerospace \
           icon.padding_left=7 \
           icon.padding_right=7 \
           icon.color=$BASE \
           background.color=$MAUVE \
           background.corner_radius=5 \
           background.height=25 \
           label.drawing=off \
           script="$PLUGIN_DIR/aerospace.sh" \
           --subscribe aerospace aerospace_workspace_change

##### Adding Left Items

sketchybar --add item chevron left \
 --set chevron icon= label.drawing=off

##### Adding Right Items

sketchybar --add item memory right \
 --set memory update_freq=5 script="$PLUGIN_DIR/memory.sh" \
           --add item wifi right \
           --set wifi update_freq=10 script="$PLUGIN_DIR/wifi.sh" \
 --subscribe wifi wifi_change \
 --add item volume right \
 --set volume script="$PLUGIN_DIR/volume.sh" \
           --subscribe volume volume_change \
           --add item battery right \
           --set battery update_freq=120 script="$PLUGIN_DIR/battery.sh" \
 --subscribe battery system_woke power_source_change \
 --add item clock right \
 --set clock update_freq=10 icon= script="$PLUGIN_DIR/clock.sh"

##### Force all scripts to run the first time (never do this in a script)

sketchybar --update
